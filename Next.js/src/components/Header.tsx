import React from 'react';

const Header: React.FC = () => (
  <header className="w-full flex items-center justify-between py-4 px-6 bg-background border-b border-border">
    <h1 className="text-2xl font-bold text-primary">تريد إكسراي</h1>
    {/* Add navigation, logo, or user menu here */}
  </header>
);

export default Header;
